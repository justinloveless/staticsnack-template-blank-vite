{
  "name": "static-site-template",
  "version": "1.0.0",
  "description": "Static site template with JSON-based content management",
  "scripts": {
    "init": "npm install && npx -p dynaloader init",
    "generate-schema": "npx -p dynaloader generate-schema",
    "add-asset": "npx -p dynaloader add-asset"
  },
  "keywords": [
    "template",
    "static-site",
    "json",
    "content-management"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dynaloader": "^1.1.0",
    "inquirer": "^8.2.5"
  }
}